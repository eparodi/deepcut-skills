1. Tap targets are at least 44x44 CSS pixels (48dp) with adequate spacing — touch and pointer coexist.
2. Hover states exist but are never required — everything is reachable by touch.
3. Scrolling behaves naturally — no scroll traps, jank, or sticky elements covering content.
4. Loading, empty, and error states are visible and give a path forward.
